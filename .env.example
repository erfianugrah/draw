# Excalidraw Self-Hosted Configuration
# =====================================
# Copy this file to .env and fill in your values

# Your domain
DOMAIN=draw.yourdomain.com

# Base URL for the app (must match DOMAIN with https://)
BASE_URL=https://draw.yourdomain.com

# Cloudflare DNS-01 challenge for Let's Encrypt
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone > DNS > Edit
CF_API_TOKEN=your_cloudflare_api_token_here
EMAIL=your_email@example.com

# Timezone (for logs and cleanup scheduling)
TZ=UTC

# Auto-cleanup settings (set to 0 to disable)
# Collaboration rooms - cleaned up after inactivity
ROOM_MAX_AGE_DAYS=30

# Shared exports (shareable links) - cleaned up after creation
EXPORT_MAX_AGE_DAYS=30

# Saved drawings - cleaned up after last access
DRAWING_MAX_AGE_DAYS=90

# How often to run cleanup (in hours)
CLEANUP_INTERVAL_HOURS=24

# =====================================
# AI Features (Optional)
# =====================================
# Enable AI-powered text-to-diagram and wireframe-to-code features
# Choose one provider: openai, anthropic, or ollama

# AI Provider (openai, anthropic, ollama)
AI_PROVIDER=openai

# --- OpenAI Configuration ---
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Models (defaults shown)
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o

# --- Anthropic Configuration ---
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=
# Models (defaults shown)
ANTHROPIC_MODEL=claude-3-haiku-20240307
ANTHROPIC_VISION_MODEL=claude-3-5-sonnet-20241022

# --- Ollama Configuration (Local AI) ---
# Run Ollama locally: https://ollama.ai
# Use host.docker.internal to access host machine from Docker
OLLAMA_HOST=
# Models (defaults shown)
OLLAMA_MODEL=llama3.2
OLLAMA_VISION_MODEL=llava

# Rate limiting (requests per day per IP)
AI_RATE_LIMIT_PER_DAY=100
