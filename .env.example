# Excalidraw Self-Hosted Configuration
# =====================================
# Copy this file to .env and fill in your values

# Your domain
DOMAIN=draw.yourdomain.com

# Base URL for the app (used for storage API and WebSocket)
# Only needed for local builds (docker-compose.yml)
BASE_URL=https://draw.yourdomain.com

# Cloudflare DNS-01 challenge for Let's Encrypt
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Zone > DNS > Edit
CF_API_TOKEN=your_cloudflare_api_token_here
EMAIL=your_email@example.com

# GitHub username (for pulling pre-built images)
# Only needed for production (docker-compose.prod.yml)
GITHUB_USER=yourgithubusername
